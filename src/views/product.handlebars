<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" integrity="sha512-..." crossorigin="anonymous" referrerpolicy="no-referrer" />
    <title>Product Details</title>
</head>
<body class="bg-black text-white">

<section class="container-fluid">
    <div class="col">
        <div class="card border-primary bg-black text-white">
            <div class="card-body text-center d-grid gap-5">
                <p class="card-text">_id: {{product._id}}</p>

                <h5 class="card-title">{{product.title}}</h5>
                <p class="card-text">{{product.description}}</p>
                <p class="card-text">Precio: {{product.price}}</p>
                <p class="card-text">Stock: {{product.stock}}</p>
                <p class="card-text">Categoría: {{product.category}}</p>
                <p class="card-text">Estado: {{product.status}}</p>
                <div>
                    {{#each product.thumbnails}}
                        <img src="{{this}}" alt="thumbnail" style="width: 250px; height: 250px;">
                    {{/each}}
                </div>
                <div class="d-flex flex-row justify-content-center gap-5">
                    <a class="btn btn-lg btn-primary" href="/products">
                        <i class="fa-regular fa-circle-left me-3"></i>Volver a productos
                    </a>
                    <button class="btn btn-lg btn-primary" onclick="addToCart('{{product._id}}')">
                        <i class="fa-solid fa-cart-shopping me-3"></i>Agregar al carrito
                    </button>
                </div>
            </div>
        </div>
    </div>
</section>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
<script src="/socket.io/socket.io.js"></script>
<script src="/js/products.js"></script>
</body>
</html>
